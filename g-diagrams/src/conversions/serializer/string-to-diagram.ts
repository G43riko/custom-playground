import { DiagramModel } from "../../model/diagram-model";

export class StringToDiagram {

    public deserialize(data: string): DiagramModel {
        const model = new DiagramModel();


        return model;
    }
}
